[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true
  exclude = ["/assets/*", "/*.js", "/*.css", "/*.json", "/*.svg", "/*.png", "/*.jpg", "/*.jpeg", "/*.gif", "/*.ico"]
